/*Environment prototype*/
/*note: Environment has flag for whether its solid or not*/
function Environment(solid ){
    this.solid = solid
}

Environment.prototype.Environment = function(){
}

Environment.prototype.getFlag = function(){
}
